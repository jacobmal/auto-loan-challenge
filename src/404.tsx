import React, { FunctionComponent } from "react";
import { Container } from "react-bootstrap";

const NotFound: FunctionComponent = () => {
    return (
        <Container><h1 className="mt-4">Page not found</h1></Container>
    );
}
 
export default NotFound;